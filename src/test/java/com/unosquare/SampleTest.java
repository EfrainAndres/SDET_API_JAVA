package com.unosquare;

public class SampleTest {
	/*Request: Metodo HTTP (GET, POST, PUT, Delete, patch)
	URL (BaseURL, Resource, QueryParams) 
	Header (ContentType)
	Body
	*/
	
	
	/*Response
	Header
	Body
	StatusCode
	*/
	
	/*Estructura basica de Rest Assured
	Given (Se utiliza para especificar el BaseURL, Headers y enviar el Body request correspondiente
	el cual es opcional teniendo en cuenta el metodo HTTP)
	When (se utiliza para realizar la solicitud HTTP, como GET, POST, PUT, DELETE, enviar el Resource de la URL)
	Then (se utiliza para verificar y validar la respuesta de la solicitud, como lo son los Headers, el StatusCode, el BodyResponse)
	*/
	
}
